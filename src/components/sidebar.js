/** @jsx React.DOM */

var React = require('react');

var Sidebar = React.createClass({
  render: function () {
    return (
        <div id="sidebar">
          <h2>Sidebar</h2>
          <p>bla bla bla</p>
        </div>
        );
  }
});

module.exports = Sidebar;
